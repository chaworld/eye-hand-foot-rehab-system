# 代辦事項

本文件記錄尚未完成的開發任務。完成後請從清單移除或勾選。

---

## Logging 系統導入（依 `.claude/rules/python_logging_style.md`）

- [ ] **建立 `logging_config.py`**：於專案根目錄新增，內容可直接複製規範檔 §5 範本（`setup_logging(subsystem, level)`、`RotatingFileHandler` 10MB × 5、輸出至 stderr + `logs/<subsystem>.log`）。
- [ ] **各子系統入口呼叫 `setup_logging(...)`**：
  - [ ] `app.py` → `setup_logging("app")`
  - [ ] `gui_module.py` → `setup_logging("eye")`
  - [ ] `gui.py` → `setup_logging("hand")`
  - [ ] `foot_gui.py` → `setup_logging("foot")`
  - [ ] `warmup_rehab/server.py` → `setup_logging("warmup")`
- [ ] **每檔頂端加入 module logger**：`logger = logging.getLogger(__name__)`（取代 `print` 與舊有的散落 logging 呼叫）。
- [ ] **既有 `print` 全面替換為 `logger.xxx`**：依規範 §3 五級準則分配級別（DEBUG/INFO/WARNING/ERROR/CRITICAL）。
- [ ] **try/except 重構**：依規範 §6 邊界政策，移除純運算函式內的 try/except，外部資源邊界補上具體例外捕捉與 log。
- [ ] **`.gitignore` 加入 `logs/`**（若尚未排除）。

## Unit Test + TDD 導入（依 `AGENTS.md` 四、5）

- [ ] **建立 `tests/` 目錄**並新增 `tests/__init__.py`（或保留為空，pytest 會自動發現）。
- [ ] **強制 unit test 覆蓋的純邏輯模組**（先寫測試 → 再補實作/重構）：
  - [ ] `tests/test_warmup_detector.py`：`compute_angle`、`compute_thigh_elevation`、`compute_trunk_lean`、`compute_abduction`
  - [ ] `tests/test_one_euro_filter.py`：`OneEuroFilter`（涵蓋穩態、突變、邊界 dt）
  - [ ] `tests/test_session_logger.py`：CSV 寫入、欄位順序、首次建檔 header、追加模式
  - [ ] `tests/test_exercises.py`：`KneeRaiseExercise`、`HipCircleExercise` phase 轉換序列、rep 計數、姿勢警告觸發
  - [ ] `tests/test_voice_assistant.py`：`_build_cache_filename` 已知/未知片語映射
- [ ] **CI / 本地驗證**：確認 `pytest` 可一鍵跑全綠。
- [ ] （可選）導入 `pytest-cov` 觀察覆蓋率。

## 文件同步

- [ ] **`README.md`** 補上 logging 與測試章節（執行方式、log 路徑、`pytest` 指令）。
- [ ] **`docs/部屬步驟.md`** 若涉及 log 檔位置或 pytest 步驟，需同步更新。

## 工具（可選）

- [ ] 引入 `black`（line-length=100）+ `ruff`，新增 `pyproject.toml` 配置。
- [ ] 純邏輯模組導入 `mypy` 嚴格檢查（GUI/CV 模組可暫不納管）。
